import LoginFormArea from './_area/LoginForm.area';

export default function LoginPage() {
  return (
    <main className="bg-foreground flex min-h-screen items-center justify-center p-4">
      <LoginFormArea />
    </main>
  );
}
